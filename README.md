# HyruleWarriors3DS_DynamicFont
本项目是3ds游戏塞尔达无双汉化时所采用的动态字库解决方案<br>
使用make命令，默认将编译成1.0版本对应的代码，<br>
使用make VER11命令，将编译为1.1版本的代码<br>
如果想要让本代码兼容于之后的版本，请修改makefile、head.S，<br>
并参考symbol目录下的符号表，为目标版本的游戏生成对应的符号表